package com.memvra.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Using Spring Boot auto-configuration for DataSource and JPA.
    // Placeholder for future customizations (pooling, properties, etc.).
}